import React from 'react';
import { Badge } from '@mui/material';

const UserBadgeNotif = ({ rows }) => {
  return <Badge color="primary" variant="dot" sx={{ left: '20px' }} />;
};

export default UserBadgeNotif;
